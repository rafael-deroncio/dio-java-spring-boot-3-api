package com.bank.core.repositories.contexts;

import com.bank.core.models.PixDetailModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PixDetailContextRepository extends JpaRepository<PixDetailModel, Integer> {
}
